Ensure register button works on home page, even if you're already pending (which should take you to /pending via /register)

Make it more obvious that hyperlinks on the "FAQ" and "about" page are clickable

When a user registers, a row appears in supabase's auth table, but not in the profiles table in supabase postgres. Something is out of sync here
